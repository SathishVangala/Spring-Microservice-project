server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Test_user
    username: postgres
    password: 1308
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
